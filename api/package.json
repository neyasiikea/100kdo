{
  "name": "100kdo-api",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute 100kdo-db --file=./schema.sql"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
